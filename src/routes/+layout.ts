// ArcGIS Maps SDK components and core API rely on browser APIs.
// Disabling SSR keeps this proof-of-concept focused on client-side map testing.
export const ssr = false;
export const prerender = true;
